apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: microservices-demo-ingress
  namespace: microservices-demo
  annotations:
    kubernetes.io/ingress.class: haproxy
spec:
  rules:
  - host: microservices.206.188.246.242.nip.io   
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: front-end
            port:
              number: 80
